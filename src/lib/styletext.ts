/**
 * Replaces basic usage of terminal colors with Node's `styleText` util.
 */

import {red, yellow, gray, black, magenta, bgYellow, bgRed} from 'kleur/colors';

import {gray, white, green, yellow, blue, cyan, magenta} from 'kleur/colors';

import {gray, white, green, yellow, blue, cyan, magenta} from 'kleur/colors';
